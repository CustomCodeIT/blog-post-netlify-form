import {NextPage} from "next";
import TheForm from "../components/the-form/the-form";

type Props = {};

const TheFormPage:NextPage<Props> = (props) => {
    return (
        <TheForm />
    );
};

export default TheFormPage;